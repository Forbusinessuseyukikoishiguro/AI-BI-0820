VSCodeでTableau用データ前処理を行うPythonコードを作成します！包括的なデータクリーニング・変換機能を持つクラスベースのコードにします。VSCode用のTableau前処理Pythonコードを作成しました！このコードは包括的なデータ前処理機能を提供する`TableauDataPreprocessor`クラスです。

## 🔧 主な機能

**データ読み込み・分析:**
- Excel/CSV自動読み込み（文字エンコーディング自動判定）
- データ概要表示・プレビュー機能
- 処理ログ記録

**クリーニング機能:**
- 空行・空列削除（閾値指定可能）
- 重複データ除去
- 欠損値処理（7種類の方法）
- テキストデータクリーニング

**変換機能:**
- データ型自動変換
- カスタムフィルタリング
- 列名変更
- Tableau最適化

## 📦 必要なライブラリ

```bash
pip install pandas numpy openpyxl
```

## 🚀 基本的な使用方法

```python
# 1. インスタンス作成とデータ読み込み
processor = TableauDataPreprocessor("your_data.xlsx")

# 2. データ概要確認
processor.show_data_info()
processor.preview_data()

# 3. 基本クリーニング実行
processor.remove_empty_rows()
processor.remove_duplicates()
processor.clean_text_data()
processor.convert_data_types()

# 4. Tableau用保存
processor.create_tableau_extract("clean_data.xlsx")
```

## 💡 高度な使用例

```python
# カスタムフィルタリング
conditions = {
    'sales_amount': {'operator': '>', 'value': 1000},
    'region': {'operator': 'in', 'value': ['Tokyo', 'Osaka']}
}
processor.filter_data(conditions)

# 欠損値を平均値で埋める
processor.fill_missing_values(strategy='mean', columns=['price'])

# 手動型変換
type_mapping = {'date_col': 'datetime', 'category_col': 'category'}
processor.convert_data_types(type_mapping=type_mapping)
```

このコードをVSCodeで実行すると、Tableau分析に最適化されたクリーンなデータが生成されます。処理ログとサマリーも自動出力されるため、データ品質の変化を追跡できます


ーーーーーーーーーーーーーーーーーー
Tableauデータ前処理ツールのGUI版を作成します！tkinterを使用して使いやすいインターフェースにします。Tableauデータ前処理ツールのGUI版を作成しました！tkinterを使用したユーザーフレンドリーなインターフェースです。

## 🎯 主な特徴

**直感的なGUI:**
- 左側：操作パネル（ファイル読み込み、処理ボタン、データ概要）
- 右側：データ表示パネル（テーブルビュー、ステータス表示）

**豊富な機能:**
- 📂 ドラッグ&ドロップファイル読み込み
- 🧹 データクリーニング（空行/列削除、重複除去）
- 🔄 データ変換（型変換、テキスト整形）
- ⚙️ ダイアログベースの詳細設定
- 💾 Excel/CSV形式エクスポート

**使いやすさ重視:**
- 非同期ファイル読み込み（UI凍結防止）
- リアルタイムデータ概要表示
- 処理ログ記録・表示
- プログレスバー表示

## 📦 必要なライブラリ

```bash
pip install pandas openpyxl
```
（tkinterは標準ライブラリなので追加インストール不要）

## 🚀 使用方法

1. **起動**: `python tableau_preprocessor_gui.py`
2. **ファイル選択**: 「📂 ファイル選択」ボタンでExcel/CSVを読み込み
3. **データ確認**: 右側パネルでデータをプレビュー
4. **処理実行**: 左側の各ボタンで必要な処理を実行
5. **保存**: 「📄 Excel保存」または「📄 CSV保存」で結果を保存

## 🛠️ 主要機能

- **空行削除**: 完全に空の行を自動削除
- **重複行削除**: 重複するレコードを除去
- **欠損値処理**: 複数の方法で欠損値を処理（ダイアログで選択）
- **テキスト整形**: 前後空白削除、連続空白正規化
- **型変換**: 数値・日付の自動変換
- **フィルタリング**: 条件指定でデータをフィルタ
- **列名変更**: 直感的な列名変更機能

このGUIツールを使えば、コードを書くことなく効率的にTableau用データの前処理が行えます！
